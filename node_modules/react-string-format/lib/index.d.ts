import * as React from "react";
type FormatType = React.ReactText | React.ReactElement;
export declare const WhiteSpaceChar = "&nbsp;";
declare function format(text: string, ...params: React.ReactText[]): string;
declare function format(text: string, ...params: FormatType[]): string | React.ReactElement;
export { format };
